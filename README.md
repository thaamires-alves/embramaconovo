
  # Convert to HTML

  This is a code bundle for Convert to HTML. The original project is available at https://www.figma.com/design/wIJusujO8qzzdYg8cwXL6T/Convert-to-HTML.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  